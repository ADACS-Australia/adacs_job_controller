# Security Warning! Do not commit this file to any VCS!
# This is a local file to speed up development process,
# so you don't have to change your environment variables.

# This is not applied to `.env.template`!
# Template files must be committed to the VCS, but must not contain
# any secret values.

# === Database ===

# These variables are consumed by both the application and mysql docker image.
# Cannot be renamed if you use mysql in docker.
# See: https://hub.docker.com/_/mysql

MYSQL_ROOT_PASSWORD=__CHANGEME__
MYSQL_DATABASE=jobcontroller
MYSQL_USER=jobcontroller
MYSQL_PASSWORD=__CHANGEME__

# Database connection settings for the application
DATABASE_HOST=db
DATABASE_PORT=3306
DATABASE_DEBUG=false

# === File Transfer Settings ===

# File download expiry time in seconds (default: 86400 = 24 hours)
FILE_DOWNLOAD_EXPIRY_TIME=86400

# File buffer and chunk sizes in bytes
# MAX_FILE_BUFFER_SIZE: Maximum buffer size (default: 52428800 = 50MB)
# MIN_FILE_BUFFER_SIZE: Minimum buffer size (default: 10485760 = 10MB)
# FILE_CHUNK_SIZE: Size of each file chunk (default: 65536 = 64KB)
MAX_FILE_BUFFER_SIZE=52428800
MIN_FILE_BUFFER_SIZE=10485760
FILE_CHUNK_SIZE=65536

# === Cluster and Access Configuration ===

# Paths to JSON configuration files
# See config/*.json.template for format examples
CLUSTER_CONFIG_FILE=/app/config/clusters.json
ACCESS_SECRET_CONFIG_FILE=/app/config/access_secrets.json
